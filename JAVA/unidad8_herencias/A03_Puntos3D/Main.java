package A03_Puntos3D;

/*Define la clase Punto, que tiene como atributos las coordenadas x e y, de tipo entero, que lo sitúan en el plano. Además del constructor, implementa el método

doble distancia(Punto otroPunto), que devuelve la distancia a otro punto que se le pasa como parámetro.

A partir de Punto, por herencia, implementa la clase Punto3D, que representa un punto en tres dimensiones y necesita una coordenada adicional z. Reimplementa el método distancia() para puntos 3D. */

/* Averiguar cómo se calcula la distancia de un punto a otro. */

public class Main {
    
}
